/**
 * Typsicherer Einstiegspunkt für das Quellregister (ADR-1).
 * Implementierung in sourceRegistry.mjs, damit auch die Node-Build-Skripte
 * (scripts/security-guards.mjs) dieselbe Quelle importieren können.
 */
export * from './sourceRegistry.mjs';
