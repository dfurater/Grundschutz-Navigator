/**
 * Typdeklaration der Arbeitsgrenze der Profile Resolution (GSPP-345).
 * Muss mit `profileResolutionBudgetLimits.mjs` übereinstimmen.
 */

/** Obergrenze der Arbeitseinheiten eines einzelnen Auflösungslaufs. */
export const WORK_UNIT_LIMIT: number;
